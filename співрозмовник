# функція, яка просить користувача ввести тему запитання
def ask_topic():
    print("Для продовження оберіть тему запитання:")
    print("1. Математика")
    print("2. Фізика")
    print("3. Філологія")
    print("4. Географія")
    choice = input("Ваш вибір (введіть номер теми): ")
    while choice not in ['1', '2', '3', '4']:
        choice = input("Некоректний вибір. Введіть номер теми від 1 до 4: ")
    if choice == '1':
        return "математика"
    elif choice == '2':
        return "фізика"
    elif choice == '3':
        return "філологія"
    else:
        return "географія"

name = input("Як вас звати?\n")

# вибір теми
topic = ask_topic()

# вивід повідомлення
print(f"Вітаю,{name} мене звати ChatBot 'Український співрозмовник'. Ви можете задати мені питання з теми {topic}.")
import random

# Запит у користувача тем, які його цікавлять
user_topics = input("Які теми з фізики вас цікавлять? Введіть через кому: ")

# Список тем з фізики, які цікавлять користувача
topics = user_topics.split(",")

# Випадковий вибір теми зі списку
random_topic = random.choice(topics)

# Вивід результату
print("Пропонуємо вам вивчити тему з фізики:", random_topic)
